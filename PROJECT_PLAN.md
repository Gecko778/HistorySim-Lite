# HistorySim Lite 项目计划

## 1. 项目定位

HistorySim Lite 是一个教学用途的历史策略模拟系统。项目目标不是制作商业级游戏，而是围绕一个可运行的回合制模拟系统，完成数据建模、核心算法、软件架构、测试验证和基础可视化界面。

当前仓库已经包含：

- `Archive/`：课程资料包，定义需求、架构、阶段任务、验收标准和测试要求。
- `hsim/`：Tauri 2 + React 19 + TypeScript + Vite 项目，目前仍接近默认模板状态。
- `hsim/src-tauri/`：Rust/Tauri 桌面端壳，目前仅包含示例 `greet` 命令。

后续开发应以 `Archive/02_案例需求与范围说明.md`、`Archive/04_系统架构与数据设计说明.md`、`Archive/05_分阶段任务指导书.md` 和 `Archive/07_验收标准与评分量规.md` 为主要依据。

## 2. 最终目标

最终系统应支持一个简化但完整的历史策略模拟闭环：

1. 显示由 30 个地区组成的地图，并用颜色区分 4 个势力。
2. 管理 30 个 Region、4 个 Faction、50 个 Person、20 个 Skill、10 个 Event。
3. 支持 5 类基础命令：`DEVELOP`、`RECRUIT`、`MOVE_ARMY`、`ATTACK`、`APPOINT`。
4. 支持回合推进、资源增长、人物任命、军队移动、战斗结算、事件触发和日志输出。
5. AI 势力每回合基于效用评分生成 1 至 3 个行动。
6. 支持保存和读取 `GameState`，并通过固定随机种子复现战斗、事件和 AI 结果。
7. 核心模拟逻辑可脱离 UI 单独测试。

## 3. 技术路线

### 前端与桌面壳

- 使用 `hsim/` 中已有的 Tauri + React + TypeScript + Vite 结构。
- React 负责地图、面板、命令输入、日志和存档界面。
- Tauri 负责桌面应用打包、本地文件读写和必要的系统能力。

### 模拟内核

优先在 TypeScript 中实现核心模拟内核，便于前端直接调用和测试：

- `src/core/`：`GameState`、回合推进、命令校验。
- `src/data/`：标准剧本数据，包括地区、势力、人物、技能、事件。
- `src/map/`：邻接图、BFS、Dijkstra、连通性检查。
- `src/economy/`：开发、征兵、资源增长。
- `src/person/`：人物属性、排序、任命效果。
- `src/battle/`：战斗公式、伤亡、归属变更、战斗日志。
- `src/ai/`：候选行动生成、效用评分、行动选择。
- `src/events/`：事件条件扫描和效果执行。
- `src/save/`：`GameState` 序列化、反序列化、版本检查。
- `src/ui/` 或 `src/components/`：地图和功能面板组件。

如果后期需要更强的本地能力，可以再把部分保存/读取能力放到 Rust/Tauri command 中；不要一开始把业务逻辑拆到 Rust，避免 UI 与内核联调成本过高。

## 4. 核心数据设计

项目应围绕 `GameState` 组织状态。建议先定义稳定类型，再填充数据和实现规则。

主要实体：

- `Region`：地区 ID、名称、所属势力、地形、人口、农业、商业、治安、城防、驻军、粮草、相邻地区。
- `Faction`：势力 ID、名称、君主、颜色、首府、金钱、粮草、威望、是否 AI。
- `Person`：人物 ID、姓名、所属势力、所在地区、年龄、状态、统率、武力、智力、政治、魅力、忠诚、野心、技能。
- `Army`：军队 ID、所属势力、所在地区、主将、兵力、士气、状态。
- `Skill`：技能 ID、名称、类型、作用范围、效果类型、效果数值。
- `Event`：事件 ID、名称、类别、触发条件、效果、日志模板。
- `Command`：命令类型、发起势力、目标对象、参数、创建回合。
- `GameState`：当前回合、日期、随机种子、实体集合、待执行命令、回合日志。

数据约束：

- 地区图必须连通。
- 地区连接关系不少于 40 条。
- 每个势力初始至少控制 3 个地区。
- 每个势力初始至少拥有 8 名人物。
- 所有 ID 引用必须能通过数据校验。

## 5. 功能分期

### 阶段一：项目骨架与领域模型

目标：把 Tauri 模板改造成 HistorySim Lite 的项目骨架。

交付：

- 更新 `README.md` 和 `hsim/README.md`。
- 建立 TypeScript 类型定义。
- 建立 `GameState`、核心实体和命令类型。
- 建立基本目录结构。
- 增加最小数据校验入口。

验收标准：

- 项目可运行。
- 能在测试或控制台中创建初始 `GameState`。
- 类型定义覆盖核心对象。

### 阶段二：标准剧本数据与地图图模型

目标：完成系统运行所需的静态数据基础。

交付：

- 30 个地区数据。
- 4 个势力数据。
- 50 个人物数据。
- 20 个技能数据。
- 10 个事件配置。
- 地区邻接表和地形成本表。
- 地图连通性检查。

验收标准：

- 数据规模达到资料包要求。
- 地图连通且连接关系不少于 40 条。
- 数据校验能发现重复 ID、无效引用和数值越界。

### 阶段三：路径、命令与回合推进

目标：形成最小可运行的模拟闭环。

交付：

- BFS 可达性查询。
- Dijkstra 最低成本路径。
- 5 类命令的数据结构和合法性校验。
- 回合推进流程。
- 命令执行日志。

验收标准：

- 能提交玩家命令。
- 能推进至少 1 个完整回合。
- 非法命令会被拒绝并写入日志。

### 阶段四：经济、人物与军队规则

目标：让地区、人物和军队状态随命令变化。

交付：

- `DEVELOP`：提升农业、商业、治安或城防。
- `RECRUIT`：消耗人口、金钱、粮草并增加兵力。
- `APPOINT`：任命太守或将领。
- `MOVE_ARMY`：根据路径和成本移动军队。
- 人物排序和能力加成。

验收标准：

- 数值增长有上限和下限。
- 人物政治属性影响开发。
- 将领统率影响后续战斗。
- 移动命令能处理不可达、资源不足和敌方阻挡。

### 阶段五：战斗、事件与 AI

目标：补齐策略模拟的核心算法。

交付：

- `ATTACK` 战斗结算。
- 战斗公式包含兵力、统率、士气、地形、城防、技能和随机波动。
- 集中随机数服务，支持固定随机种子。
- 事件扫描、触发和效果执行。
- AI 候选行动、效用评分和行动选择。
- AI 决策日志。

验收标准：

- 战斗产生胜负、伤亡、士气变化和地区归属变化。
- 事件能改变地区、势力、人物或军队状态。
- AI 势力每回合能生成 1 至 3 个非纯随机行动。
- 同一初始状态、命令序列和随机种子可复现结果。

### 阶段六：UI 闭环

目标：让用户通过界面完成主要操作。

交付：

- Map View：显示 30 个地区、势力颜色、地区选择。
- Region and Faction Panel：查看地区和势力详情。
- Person and Appointment Panel：查看人物、排序、任命。
- Command Panel：输入 5 类基础命令。
- Turn Log and Save Panel：推进回合、查看日志、保存和读取。

验收标准：

- 用户能从界面选择地区并查看详情。
- 用户能输入命令并推进回合。
- 用户能看到战斗、事件、AI 和资源变化日志。
- 用户能保存并读取状态。

### 阶段七：测试、文档与答辩准备

目标：达到资料包的最终验收要求。

交付：

- 单元测试：地图、路径、资源、战斗、AI、事件、命令校验。
- 集成测试：完整回合、20 回合连续模拟、存档恢复后继续模拟。
- 边界测试：非法攻击、资源不足、地图不连通、人物死亡后任命等。
- 复现测试：固定随机种子下的战斗、事件、AI 排序和 20 回合摘要。
- `ARCHITECTURE.md`。
- `TEST_REPORT.md`。
- 用户操作说明。
- 演示脚本。

验收标准：

- 系统能连续推进至少 20 回合。
- 核心算法有测试用例。
- 文档能解释算法、架构、测试和使用流程。

## 6. 建议里程碑

| 里程碑 | 目标 | 主要产出 |
|---|---|---|
| M1 | 项目骨架完成 | 类型定义、目录结构、初始 GameState |
| M2 | 数据和地图完成 | 标准剧本数据、邻接图、数据校验 |
| M3 | 最小模拟闭环 | 命令校验、回合推进、日志 |
| M4 | 核心规则完成 | 经济、人物、移动、战斗 |
| M5 | AI 与事件完成 | AI 评分、事件系统、复现随机数 |
| M6 | UI 闭环完成 | 地图、面板、命令、日志、存档 |
| M7 | 验收准备完成 | 测试报告、架构文档、演示脚本 |

## 7. 测试计划

优先引入 Vitest 作为 TypeScript 单元测试框架。测试重点应放在模拟内核，而不是只测 React 组件。

必要测试：

- 地图连通性测试。
- BFS 可达性测试。
- Dijkstra 最低成本路径测试。
- `DEVELOP` 公式测试。
- `RECRUIT` 资源消耗测试。
- `APPOINT` 任命影响测试。
- `ATTACK` 战斗结算测试。
- AI 评分排序测试。
- 事件触发测试。
- 存档读档一致性测试。
- 20 回合连续模拟测试。

质量要求：

- 每个核心模块都应有独立测试入口。
- 固定随机种子的测试必须记录 seed、初始数据版本和命令序列。
- 所有关键状态变化都应写入日志，便于答辩解释。

## 8. 当前风险与处理建议

| 风险 | 影响 | 建议 |
|---|---|---|
| 当前代码仍是 Tauri 模板 | 无法体现项目主题 | 先替换 UI 首页并建立核心目录 |
| 数据规模较大 | 手写数据容易出现引用错误 | 先写数据校验，再补齐完整数据 |
| UI 与模拟逻辑混杂 | 后期难测试、难维护 | 模拟内核必须独立于 React 组件 |
| AI、战斗、事件同时推进 | 复杂度容易失控 | 先做可解释的简单公式，再逐步调参 |
| 随机过程不可复现 | 测试和答辩难验证 | 从第一版战斗开始使用集中随机数服务 |
| 存档格式变化 | 后期读档失败 | 存档中加入 schema/version 字段 |

## 9. 下一步建议

建议从以下任务开始：

1. 更新 `hsim/src/App.tsx`，把默认 Tauri 欢迎页替换成 HistorySim Lite 的基础布局。
2. 新建 `hsim/src/core/types.ts`，定义 `Region`、`Faction`、`Person`、`Army`、`Skill`、`Event`、`Command`、`GameState`。
3. 新建 `hsim/src/data/scenario.ts`，先放入少量样例数据，打通状态展示。
4. 新建 `hsim/src/core/createInitialState.ts`，生成初始 `GameState`。
5. 新建 `hsim/src/core/validation.ts`，实现 ID 唯一、引用有效、地图连通等数据校验。
6. 安装并配置 Vitest，先覆盖数据校验和地图连通性。

完成上述内容后，项目就能从“模板工程”进入“可持续实现”的状态。
